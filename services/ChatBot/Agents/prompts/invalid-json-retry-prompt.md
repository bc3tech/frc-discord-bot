Your previous response was not valid JSON for the required schema.
This is repair attempt [[RETRY_ATTEMPT]] of [[MAX_INVALID_JSON_RETRIES]].

JSON parser exception:
[[JSON_EXCEPTION]]

Previous payload:
[[PREVIOUS_PAYLOAD]]

Re-emit the same intended result as valid JSON only.
Keep every JSON string value on a single line; if you need a line break, escape it as \n.
Do not add commentary, markdown fences, or any text outside the JSON object.
